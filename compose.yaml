services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - '8080:80'

    depends_on:
      - api

  api:
    build:
      context: .
      dockerfile: Dockerfile.api

    expose:
      - '3000'

    volumes:
      - ./db.json:/app/db.json
